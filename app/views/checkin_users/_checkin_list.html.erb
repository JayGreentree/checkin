<table id="checkin-users-table" class="table table-striped table-hover" data-toggle="table" data-search="true">
    <thead>
        <tr>
            <th></th>
            <th data-sortable="true">Checkin Time</th>
            <th data-sortable="true">Name</th>
            <th data-visible="false">Andrew ID</th>
            <th data-sortable="true">Residence</th>
            <th data-sortable="true">Program</th>
            <th data-visible="false">Checked In By</th>
            <th data-sortable="false" data-searchable="false">Phone</th>
            <th data-visible="false" data-searchable="false">Counsellors</th>
            <th data-visible="false" data-searchable="false">Roommate</th>
        </tr>
    </thead>
    <tbody>
        <%= render partial: "checkin", collection: filter_checkins(checkin_list, params[:filter]) %>
    </tbody>
</table>

<script>
/* function detailFormatter(row){
        var html = [];
        html.push(row[2] );
        return html.join('');
 }*/
 
 /*
(function() {
 $("table").on("click-row.bs.table",function(e,row,element) {
 
     that = $("table");
     var $this = $(this),
         index = element.data('index')

         console.log("element.data[index]");
     element.toggleClass("detail-view-parent");
     element.find('td').toggleClass("bg-primary");
     element.removeClass("danger");
         if (element.next().is('tr.detail-view')) {
             element.next('tr.detail-view').find('div').slideUp("slow",function(){$(this).parent().parent().remove();});             
         } else {

             element.after('<tr class="detail-view"><td colspan="' + element.find('td').length + '"><div><button type="button" class="close" data-dismiss="detail-view" aria-label="Close"><span aria-hidden="true">&times;</span></button>' +
                           detailFormatter(row) + '</div></td></tr>');
             element.next('tr.detail-view').find('div').slideDown();
             element.next('tr.detail-view').find('button.close').on("click",function(){
                 $("table").trigger("click-row.bs.table",[row,element]);
             });
     }
 });
    }).call(this);
*/
</script>
